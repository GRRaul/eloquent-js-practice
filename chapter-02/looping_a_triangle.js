
// Exercise 01: Looping a Triangle
// This programme builds a triangle using the "#" character.
// It starts with one "#", prints it to the console, and then adds another "#"
// on each loop iteration.
// The loop continues while the string length is less than or equal to 7.
// Build and print a triangle by increasing the string length on each loop iteration.

let text = '';

while (text.length < 7 ) {
    console.log(
        text += '#'
    )
}


   


